[project]
name = "linkedlistvisualizer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mt-linked-list @ git+https://github.com/mturnbo/LinkedList.git",
    "pygame",
    "pyside6>=6.11.2",
]

[dependency-groups]
dev = [
    "mypy",
    "pytest",
    "ruff",
]
